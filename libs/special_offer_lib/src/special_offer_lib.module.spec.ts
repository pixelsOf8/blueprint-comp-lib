import { DemoSpecialOfferLibModule } from './demo-special_offer-lib.module';

describe('DemoSpecialOfferLibModule', () => {
  it('should work', () => {
    expect(new DemoSpecialOfferLibModule()).toBeDefined();
  });
});
